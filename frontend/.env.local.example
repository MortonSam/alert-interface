# Points to the FastAPI backend (used by next.config.ts rewrites)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Sentry error tracking (get DSN from sentry.io → Project Settings → Client Keys)
NEXT_PUBLIC_SENTRY_DSN=
